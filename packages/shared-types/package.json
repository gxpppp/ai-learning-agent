{
  "name": "@ai-tutor/shared-types",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "biome check src/",
    "check-types": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "devDependencies": {
    "@ai-tutor/typescript-config": "workspace:*",
    "typescript": "^5.7.0"
  }
}
